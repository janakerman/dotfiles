# Dotfiles

Install `Homebrew`.

Then, `cd` into this repository:

```
# Sync dotfiles
source bootstrap.sh
# Install brew packages
./brew.sh
```

## Overrides

* Git config with `~/.gitconfig.local`.
